# PWA単語学習アプリ 仕様書

## 1. アプリ概要

英単語を「見るだけ」で終わらせず、

**インプット → アウトプット → 答え合わせ → 次のインプット**

を繰り返すことで、記憶の定着を狙う単語学習PWA。

コンセプトは、

> 見れば分かる、ではなく、自分で書ける状態をつくる。

日本語でも、読むだけでは漢字を書けなくなることがある。
英単語も同じで、入力だけではなく「思い出して書く」工程を毎回挟む。

---

## 2. 想定プラットフォーム

- PWA
- iPhone Safari対応
- ホーム画面追加対応
- PCブラウザでも利用可能
- App Store配布は不要
- URL共有だけで利用可能

---

## 3. 基本学習フロー

### ① 単語をおぼえよう💡

画面タイトル：

**単語をおぼえよう💡**

表示例：

```text
Dog
```

ユーザーは英単語を確認して覚える。

ボタン：

```text
【次へ】
```

【次へ】をタップすると、アウトプット画面へ進む。

---

### ② 単語を書こう✏️

画面タイトル：

**単語を書こう✏️**

表示例：

```text
犬
```

入力欄：

```text
[              ]
```

ユーザーが英単語を入力する。

例：

```text
DOC
```

ボタン：

```text
【answer】
```

【answer】をタップすると正誤判定画面へ進む。

---

### ③ 正解・不正解を表示

#### 正解の場合

```text
✅ 正解！

Dog
```

#### 不正解の場合

例：

ユーザー入力：

```text
DoC
```

正解：

```text
Dog
```

表示イメージ：

```text
❌ 不正解

DoC
  ↑
  間違えた文字を赤色表示

正解：Dog
```

可能であれば、正解との比較結果から
**間違っている文字だけ赤色で表示する。**

表示後、次の単語へ進み、①へ戻る。

---

## 4. 基本ループ

```text
① 英単語を見る
↓
② 日本語を見て英単語を書く
↓
③ 正誤判定
↓
次の単語
↓
①へ戻る
```

このループを学習の中心とする。

---

## 5. 正誤判定ルール

初期仕様では、大文字・小文字は区別しない。

以下はすべて正解扱い：

```text
Dog
dog
DOG
```

以下は不正解：

```text
Doc
Dgo
Dogg
```

判定時は入力値と正解値の両方を小文字化して比較する。

ただし、結果表示ではユーザーが入力した文字列をそのまま表示する。

---

## 6. 単語データ

初期版では、単語データをJavaScript内またはJSONで保持する。

例：

```json
[
  {
    "english": "Dog",
    "japanese": "犬"
  },
  {
    "english": "Cat",
    "japanese": "猫"
  },
  {
    "english": "Apple",
    "japanese": "りんご"
  }
]
```

---

## 7. 画面構成

### 画面A：インプット

表示内容：

- タイトル「単語をおぼえよう💡」
- 英単語
- 【次へ】ボタン

### 画面B：アウトプット

表示内容：

- タイトル「単語を書こう✏️」
- 日本語
- 英単語入力欄
- 【answer】ボタン

入力欄では、可能であればEnterキーでも回答できるようにする。

### 画面C：結果

表示内容：

- 正解 / 不正解
- ユーザー入力
- 正解単語
- 間違い文字の強調表示
- 【次へ】ボタン

【次へ】で次の単語の画面Aへ移動。

---

## 8. UI / デザイン方針

見た目は、勉強アプリらしくしすぎず、
シンプルで親しみやすいデザインにする。

希望：

- スマホ中心
- 大きな文字
- 余白を広めにする
- ボタンを押しやすくする
- カードUI
- やわらかい色
- 正解は緑系
- 不正解は赤系
- iPhoneのホーム画面から起動した際、アプリらしく見えるデザイン
- 縦画面を基本とする
- レスポンシブ対応

---

## 9. PWA要件

最低限、以下を実装する。

- `manifest.json`
- Service Worker
- ホーム画面追加対応
- アプリアイコン
- standalone表示
- オフラインでも基本画面を開ける構成

---

## 10. データ保存

初期版ではLocalStorageを利用する。

保存候補：

- 現在の単語位置
- 正解数
- 不正解数
- 各単語の正誤履歴
- 間違えた回数

ページを閉じても学習履歴が残るようにする。

---

## 11. 今後追加したい機能

初期版では必須ではないが、後から追加できる設計にする。

### 間違えた単語の再出題

間違えた単語ほど再び出やすくする。

### 苦手単語モード

間違い回数が多い単語だけを学習する。

### 学習履歴

例：

```text
今日
正解：18
不正解：4
正答率：81.8%
```

### 単語追加

ユーザー自身が、

- 英単語
- 日本語

を登録できるようにする。

### 単語編集・削除

登録済み単語を変更可能にする。

### CSV / JSONインポート

大量の単語を一括登録できるようにする。

---

## 12. 初期版の優先順位

最初は機能を増やしすぎず、以下だけを確実に動かす。

1. 英単語を表示
2. 【次へ】
3. 日本語を表示
4. 英単語入力
5. 【answer】
6. 正誤判定
7. 間違った文字の強調
8. 次の単語へ
9. LocalStorage保存
10. PWA化

---

## 13. 技術構成案

シンプルな構成を優先する。

```text
index.html
style.css
app.js
manifest.json
service-worker.js
icons/
```

フレームワークは必須ではない。

まずは

- HTML
- CSS
- JavaScript

のみで作成してよい。

---

## 14. Codexへの実装指示

この仕様書を元に、PWAとして動作する完成版を作成すること。

### 実装時の条件

- iPhone Safariで使いやすいUI
- ホーム画面追加を想定
- 画面遷移は高速
- 入力後の正誤判定が分かりやすい
- 不正解時、可能な範囲で誤字部分を赤色表示
- LocalStorageで進捗保存
- 外部ライブラリは極力使わない
- コードは読みやすく分割する
- エラー処理を入れる
- コメントを適切に入れる
- 初回起動時にサンプル単語を数件入れる
- 後から単語追加機能を実装しやすい構造にする

---

## 15. アプリの思想

このアプリで最も重要なのは、

**インプットだけで終わらせないこと。**

英単語を見る。

その直後に、日本語だけを見て、
自分の記憶から英単語を書き出す。

そして答え合わせをする。

この

```text
覚える
↓
思い出す
↓
書く
↓
確認する
```

という循環を、毎回必ず行う。

単なる単語カードではなく、
**「思い出す練習」を中心にした学習アプリ**として設計する。
